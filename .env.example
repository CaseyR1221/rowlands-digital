# Resend credentials for the /contact form. Copy to .env.local and fill in.
# The API key is server-only — never prefix these with NEXT_PUBLIC_.
RESEND_API_KEY=
RESEND_FROM_EMAIL=website@mail.rowlandsdigitalworks.com
RESEND_CONFIRMATION_FROM_EMAIL=casey@mail.rowlandsdigitalworks.com
